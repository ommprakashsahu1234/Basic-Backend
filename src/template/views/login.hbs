<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="../public/css/style.css">
</head>
<body>
    {{>UnlogHead}}

    <div class="title-text" align="center">
      <h1>
        LOGIN
      </h1>
    </div>

    <form action="/" method="POST" class="login-form">
  <div class="form-container">
    
    <label for="username">Username:</label>
    <input type="text" id="username" name="username" required placeholder="Enter your username" required>
      
    <label for="password">Password:</label>
    <input type="password" id="password" name="password" required placeholder="Enter your password" required>

    <button type="submit" class="submit-btn">Login</button>
  </div>
</form>

    {{>footer}}
</body>
<style>
    /* Form container styling */
form {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  margin-top: 10px;
  margin-bottom: 10px;
  color: white;
}

.form-container {
  background-color: #03083de0;
  padding: 20px;
  border-radius: 15px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  width: 100%;
  max-width: 400px; /* Form will be centered and won't stretch too wide */
  border: 4px solid greenyellow;
}

/* Label styling */
label {
  display: block;
  font-size: 14px;
  margin: 10px 0 5px;
  color: #ffffff;

}

label:hover {
  background-color: greenyellow;
  color: black;
  max-width: fit-content;
  padding: 0 2px; /* Adds 2px padding on both sides */
  border-radius: 3px;
}

/* Input and Textarea styling */
input, textarea {
  width: 100%;
  padding: 10px;
  font-size: 16px;
  margin-bottom: 15px;
  border: 1px solid #ccc;
  border-radius: 5px;
  box-sizing: border-box;
}

textarea {
  resize: vertical;
  min-height: 80px;
}

/* Button styling */
button.submit-btn {
  width: 100%;
  padding: 12px;
  background-color: #06022d;
  color: white;
  font-size: 16px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  transition: background-color 0.3s;
}

button.submit-btn:hover {
  background-color: #1885de;
}

/* Focus styling for inputs and textarea */
input:focus, textarea:focus {
  border-color: #1885de;
  outline: none;
}

/* Placeholder styling */
input::placeholder, textarea::placeholder {
  color: #888;
}

/* Responsive styling for mobile */
@media (max-width: 768px) {
  .form-container {
    padding: 15px;
    width: 90%;
    max-width: 350px; /* Ensure form doesn't stretch too wide on smaller devices */
  }

  button.submit-btn {
    font-size: 14px;
  }

  label {
    font-size: 13px;
  }
}

</style>
</html>